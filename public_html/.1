mysqlite_query($connect, "INSERT INTO passengers values ('$newName', '$newMName','$newLName','$newSSN')")
<?php

var_dump($_POST[f_name]);
var_dump($_POST[l_name]);
echo var_dump$_POST[ssn])'

//on success, redirect to passenger list
//on fail redirect to form and indicate errors
//but now submit should indicate update
//the click on update with direct toan updatePAssenger.php to update the info



?>
